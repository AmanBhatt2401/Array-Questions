#include<bits/stdc++.h>
using namespace std;

int findMIN(int arr[],int n)
{
    return *min_element(arr,arr+n);
}
int main()
{
    int arr[]={4,5,6,1,2,78,3,5,97};
    int n=sizeof(arr)/sizeof(arr[0]);
    
    for(int i=0;i<n;i++)
    cout<<arr[i]<<" ";
    cout<<endl<<"MIN Elment is:";
    
    cout<<findMIN(arr,n);
}
